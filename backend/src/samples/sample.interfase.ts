export type sample = {
    itemNo : string,
    size: string,
    section: string,
    image: string,
    remarks: string,
    price: string,
    date: string,
    status: "on progress" | "Complete"
}